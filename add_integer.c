#include<stdio.h>
 int main () {
     int a,b,sum;
      printf("Enter the first number to add\n");
      scanf("%d",&a);
      printf("Enter the second number to add\n");
      scanf("%d",&b);
      
      sum=a+b;
      printf("\n%d + %d is %d",a,b,sum);
     return 0;
     
 }